<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.exoplatform.platform</groupId>
		<artifactId>exo.platform.component.edition</artifactId>
		<version>3.5.0-CR1</version>
	</parent>
	<artifactId>exo.platform.component.edition.parent</artifactId>
	<packaging>pom</packaging>
	<name>eXo Platform - Edition Parent POM</name>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<!--version>1.6</version-->
				<executions>
					<execution>
						<id>compile</id>
						<phase>generate-sources</phase>
						<configuration>
							<target>
								<!-- defines the needed properties -->
								<property name="platform.edition" value="${platform.edition}" />
								<property name="packagePath" value="src/main/java/org/exoplatform/platform/edition" />
								<property name="packageName" value="org.exoplatform.platform.edition" />
								<property name="className" value="PlatformEdition" />
								<property name="fileName" value="${className}.java" />
								<!-- generates the class -->
								<echo file="${basedir}/${packagePath}/${fileName}" message="package ${packageName};${line.separator}" />
								<echo file="${basedir}/${packagePath}/${fileName}" append="true" message="public final class ${className} {${line.separator}" />
								<echo file="${basedir}/${packagePath}/${fileName}" append="true" message=" public static String getEdition() { return &amp;quot;${platform.edition}&amp;quot; ;}${line.separator}" />
								<echo file="${basedir}/${packagePath}/${fileName}" append="true" message="}${line.separator}" />
							</target>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
