
This section describes how to copy a work done on the eXo Platform server, such as creating navigations, node types and templates, to another Platform servers. To copy the whole site, you need to do the following actions:

h2. Copy the site content folder with its version history

*Step 1:* Go to the *Sites Management* drive.

*Step 2:* Open the site node, for example "acme".

*Step 3:* Click *Export Node* to export the node with its version history as below:

!how_to_copy_a_site/export_site_nodes2.png!

*Step 4:* Select *Export* or *Export version history* to perform exporting.

*Step 5:* Click *Import Node* to open the *Import Node* form. 

*Step 6:* Select the exported nodes and version history to be imported.

!how_to_copy_a_site/import_site_nodes2.png!

One pop-up message will appear to inform that you have imported successfully as below: 
!how_to_copy_a_site/import_successfully.png!

h2. Copy navigation nodes of sites

*Step 1:* Add a new drive to both target and source servers.

!how_to_copy_a_site/add_portal_system_drive.png!

*Step 2:* Export the navigation node.

!how_to_copy_a_site/export_site_navigations.png!

*Step 3:* Import the nodes navigation.

!how_to_copy_a_site/import_site_navigations.png!

h2. Copy the template of node type

{note}
If you have some specific JCR namespaces and node types, you need to import them into the new server.
{note}

*Step 1:* Add the *System* drive to both servers.

!how_to_copy_a_site/add_system_drive.png!

*Step 2:* Open system:/jcr:system/exo:namespaces/{noformat}{namespace_name}{noformat}, and export it.

!how_to_copy_a_site/export_namespace.png!

*Step 3:* Open system:/jcr:system/exo:namespaces/, and import the exported file as described in *Step 2*.

!how_to_copy_a_site/import_namespace.png!

*Step 4:* Open system:/jcr:system/jcr:nodetypes/{noformat}{node_type}{noformat}, and export it.

!how_to_copy_a_site/export_node_type.png!

*Step 5:* Open system:/jcr:system/jcr:nodetypes/, and import the exported file as described in *Step 4*.

!how_to_copy_a_site/import_nodetype.png!

h2. Copy WCM templates

{note}
If you have some specific WCM (CLV/PCLV) views and/or templates of node types, you will need to import them into the new server.
{note}


*Step 1:* Open the *DMS Administration* drive.

*Step 2:* Open dms-system:/exo:ecm/templates/{noformat}{node_type}{noformat}, and export it.

!how_to_copy_a_site/export_node_type_templates.png!

*Step 3:* Open dms-system:/exo:ecm/templates/, and import the exported file.

!how_to_copy_a_site/import_node_type_templates.png!

Also, for CLV/PCLV templates, you can find all template views defined in the dms-system:/exo:ecm/views path with:

* userviews: this folder contains views of Sites Explorer with a set of actions.
* templates: where you can find all gtmpl templates of:
** Category Navigation Portlet templates.
** Content List Viewer (CLV) templates and its paginator templates.
** content-browser templates (Deprecated Portlet).
** ecm-explorer templates define how to display nodes in the Sites Explorer portlet, such as CoverFlow template, IconView template.
** Parameterized Content List Viewer (PCLV) templates and its paginator templates.
** WCM Advanced Search is used in the WCM Search portlet to define the form, layout, result and result's paginator.

If you want to reuse one of the non-predefined templates above, simply export and import it into the new server at the same place.

!how_to_copy_a_site/WCM_templates.png!

h2. Copy a Taxonomy tree

By importing the whole site as described in the *Copy the site content folder with its version history* section, you will also have the Taxonomy tree imported. The default location where the site's Taxonomy is placed in a sub-folder named *category*. So, you do not need to export or import them because this step is automatically done. But the Taxonomy tree definition is still not fully imported in the new server. What you now need to do is to add this Taxonomy Tree definition by following those steps:

*Step 1:* Open the *DMS Administration* drive in the new server.

*Step 2:* Go to dms-system:/exo:ecm/exo:taxonomyTrees/definition/.

*Step 3:* Add a symlink to the *Taxonomy Tree Root Node*, for example "collaboration:/sites content/live/acme/categories/acme".

!how_to_copy_a_site/select_TaxonomyTree_rootNode.png!

The name of symlink is displayed as "acme".

!how_to_copy_a_site/select_TaxonomyTree_rootNode2.png!

The symlink is generated as below:

!how_to_copy_a_site/select_TaxonomyTree_rootNode3.png!

In some cases, to see changes, you need to clean cache by disconnecting or restarting the server. 

!how_to_copy_a_site/TaxonomyTree_added_successfully.png!


h2. Copy metadata templates

*Step 1:* Open the *DMS Administration* drive in the new server.

*Step 2:* Go to /exo:ecm/metadata/{noformat}{meta_data_name}{noformat}.

*Step 3:* Export and re-import it in the same location in the new server.


h2. Copy queries

*Step 1:* Open the *DMS Administration* drive in the new server.

*Step 2:* Go to "/exo:ecm/queries/{noformat}{query_name}{noformat}".

*Step 3:* Export and re-import it in the same location in the new server.

h2. Copy scripts

*Step 1:* Open the *DMS Administration* drive in the new server.

*Step 2:* Go to "/exo:ecm/scripts/ecm-explorer/".

You will find three folders referring to the three types of groovy scripts in eXo Platform, including:

** action: The action scripts are launched when an ECM action triggers them. For more information, refer to *Actions Concept*.

** interceptor: Interceptor scripts are triggered before and/or after the JCR node is saved, or when a node is created or edited. They are used to either validate the value entered in a form or to manipulate the newly created node, for example, to map the new node with a forum thread or any other type of discussion areas.

** widget: Widget scripts are used to fill widgets, such as a selectbox in a dynamic way.

*Step 3:* Export your customized script in the same location in the new server.

h2. Copy drive configurations

*Step 1:* Open the *DMS Administration* drive in the new server.

*Step 2:* Go to /exo:ecm/exo:drives/{noformat}{drive_name}{noformat}.

*Step 3:* Export and re-import it in the same location in the new server.

h2. Copy gadgets

*Step 1:* Open the drive that points into *Portal-System* Workspace.

*Step 2:* Go to your gadget "portal-system:/production/app:gadgets/{noformat}{gadget_namee}{noformat}".

*Step 3:* Export and re-import it in the same location in the new server.

h2. Restart server

After importing the site navigation nodes, the site looks like:

!how_to_copy_a_site/acme_site_after_import.png!

So, you need to restart the server first. The site looks like:

!how_to_copy_a_site/acme_site_after_import_and_restart.png!
