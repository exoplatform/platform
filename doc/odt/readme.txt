To run, you have to modify the build.properties to point to your local Open Office executable.
PDF generation is started by mvn install from the current folder. To stop Open Office, maven/ant uses the killall command from Linux systems, it may not work well on Windows...
