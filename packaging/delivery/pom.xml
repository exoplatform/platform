<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.exoplatform.platform</groupId>
    <artifactId>exo.platform.packaging</artifactId>
    <version>3.0.1-SNAPSHOT</version>
  </parent>
  <artifactId>exo.platform.packaging.delivery</artifactId>
  <packaging>pom</packaging>
  <name>eXo Platform - Delivery</name>
  <profiles>
    <profile>
      <id>distrib</id>
      <dependencies>
        <!-- Tomcat server -->
        <dependency>
          <groupId>${project.groupId}</groupId>
          <artifactId>exo.platform.packaging.assembly</artifactId>
          <classifier>tomcat</classifier>
          <version>${project.version}</version>
          <type>zip</type>
        </dependency>
        <!-- CHAT server -->
        <dependency>
          <groupId>${project.groupId}</groupId>
          <artifactId>exo.platform.packaging.assembly</artifactId>
          <classifier>chatserver</classifier>
          <version>${project.version}</version>
          <type>zip</type>
        </dependency>
      </dependencies>
      <build>
        <finalName>eXoPlatform-${project.version}</finalName>
        <plugins>
          <plugin>
            <artifactId>maven-assembly-plugin</artifactId>
            <executions>
              <execution>
                <id>distribution</id>
                <phase>package</phase>
                <goals>
                  <goal>single</goal>
                </goals>
                <configuration>
                  <descriptors>
                    <descriptor>src/main/assembly/platform-distribution.xml</descriptor>
                  </descriptors>
                  <appendAssemblyId>false</appendAssemblyId>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
