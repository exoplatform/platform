<project>
  <parent>
    <groupId>org.exoplatform.portal</groupId>
    <artifactId>config</artifactId>
    <version>2.0</version>
  </parent> 

  <modelVersion>4.0.0</modelVersion>
  <artifactId>exo.portal.web.portal</artifactId>
  <packaging>war</packaging> 
  <version>${org.exoplatform.portal.version}</version>
  <name>eXo Portal web module</name>
  
  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>compile</id>
            <phase>compile</phase>
            <configuration>
              <tasks>
		            <copy todir="${basedir}/target/${project.build.finalName}/WEB-INF/classes">
		              <fileset dir="${basedir}/src/main/webapp/WEB-INF/classes">
		                <include name="**/*_en.properties"/>
		              </fileset>
		              <globmapper from="*_en.properties" to="*.properties"/>
		            </copy>
              </tasks>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
